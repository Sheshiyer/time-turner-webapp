// DISABLED VIEW TRANSITION FOR NOW - TO BE RE-ENABLED IN FUTURE
/*
import React from 'react';

interface ViewTransitionProps {
  show: boolean;
  children: React.ReactNode;
}

const ViewTransition: React.FC<ViewTransitionProps> = ({ show, children }) => {
  return (
    <div className={`transition-opacity duration-500 w-full h-full ${show ? 'opacity-100' : 'opacity-0'}`}>
      {children}
    </div>
  );
};

export default ViewTransition;
*/
